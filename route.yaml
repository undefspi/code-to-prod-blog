kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: reverse
  namespace: reverse-words-stage
  labels:
    app: reversewords
    app.kubernetes.io/instance: reverse-words-stage
spec:
  host: reverse-reverse-words-stage.apps-crc.testing
  to:
    kind: Service
    name: reversewords
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None